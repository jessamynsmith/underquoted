---
machine:
    python:
        version: 3.4.2

test:
    override:
        - flake8
        - coverage run manage.py test
        - PYTHONPATH=. coveralls

deployment:
    production:
        branch: master
        commands:
            - sh scripts/deploy_to_heroku.sh circle
